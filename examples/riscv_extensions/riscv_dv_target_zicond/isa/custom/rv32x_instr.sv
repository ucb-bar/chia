// VEXT Zicond: no RV32X variants — our core is 64-bit, so czero.eqz/czero.nez are
// defined in rv64x_instr.sv (group RV64X). This file only overrides riscv-dv's
// stub so the whole isa/custom/ set is consistent.
